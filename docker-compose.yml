services:
  api:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./backend/data:/app/data:ro

  frontend:
    build: ./multilingual-support-fronend
    ports: ["4300:80"]
    depends_on: [api]
